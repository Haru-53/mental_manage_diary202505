<!DOCTYPE html>
<html>
  <head>
    <title>mental_manage_diaryメンタル管理日記</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c&display=swap" rel="stylesheet">
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
      body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }
      .site-header {
        /* ヘッダーのスタイルをここに */
      }
      .main-content-wrapper {
        flex: 1;
        display: flex;
        flex-direction: column;
        padding-top: 80px;    /* ↑上の余白を大きく */
        padding-bottom: 80px; /* ↓下の余白を大きく */
      }
      .content-container {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 15px rgba(0,0,0,0.1);
        padding: 30px;
        margin-bottom: 20px;
      }
      .site-footer {
        margin-top: auto;
        /* フッターのスタイルをここに */
      }
      .footer-link {
        text-decoration: none;
        color: #6c757d;
        margin: 0 15px;
        display: inline-flex;
        align-items: center;
      }
      .footer-link i {
        margin-right: 5px;
      }
      .footer-link:hover {
        color: #0d6efd;
      }
      .date-header {
        font-size: 1.5rem;
        font-weight: bold;
        color: #343a40;
      }
    </style>
  </head>

  <body class="bg-light">
    <!-- ヘッダー -->
    <header class="site-header">
      <div class="header-container">
        <div class="header-left">
          <%= link_to "戻る", "#", class: "back-button", onclick: "history.back(); return false;" %>
          <%= link_to "ホーム", root_path, class: "home-button" %>
        </div>
        
        <div class="header-right">
          <% if user_signed_in? %>
            <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "logout-button" %>
          <% end %>
          
          <div class="hamburger-menu">
            <span class="hamburger-icon">☰</span>
            <div class="dropdown-menu">
              <ul>
                <li><%= link_to "メニュー1", "#" %></li>
                <li><%= link_to "メニュー2", "#" %></li>
                <li><%= link_to "メニュー3", "#" %></li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </header>

    <!-- メインコンテンツ領域 - 大きな上下の余白を追加 -->
    <div class="main-content-wrapper">
      <div class="container">
        <% if notice %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= notice %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        
        <div class="content-container">
          <%= yield %>
        </div>
      </div>
    </div>

    <!-- フッター -->
    <footer class="site-footer">
      <div class="footer-container">
        <div class="footer-links">
          <%= link_to "お問い合わせ", contact_path, class: "footer-link" %>
          <%= link_to "お知らせ", announcements_path, class: "footer-link" %>
        </div>
        <div class="footer-info">
          <p>最終更新日: <%= Date.today.strftime('%Y年%m月%d日') %></p>
          <p>author:haru</p>
        </div>
      </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

<%# フラッシュメッセージの表示 %>
<% flash.each do |key, message| %>
  <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %>">
    <%= message %>
  </div>
<% end %>
